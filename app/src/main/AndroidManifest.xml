<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lebelleami.com.beam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/beamicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/beamicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Controller.SplashScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.IntroScreen" />
        <activity android:name=".Controller.Home" />
        <activity
            android:name=".Controller.MovieDetails"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".Controller.TvDetails"
            android:theme="@style/AppTheme2" />
        <activity android:name=".Controller.Settings"
            android:theme="@style/Base.AppSettings">
        </activity>
    </application>

</manifest>